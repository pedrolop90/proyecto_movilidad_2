
  /*
  var todos=document.querySelectorAll(".contenedor_navegacion");
  var ultimo;
  var encontrado=false;
  for (var i = 0; i < todos.length; i++) {
    if(todos[i].className.indexOf("seleccionado_navegacion")!=-1){
        ultimo=todos[i];
        todos[i].className="thumbnail";
        console.log(ultimo);
    }
    if(todos[i].href==window.location.href){
      encontrado=true;
      todos[i].className+=" seleccionado_navegacion";
    }
  }
  if(!encontrado){
    ultimo.className+=" seleccionado_navegacion";
  }
*/
